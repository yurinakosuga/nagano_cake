<h2>ジャンル一覧・追加</h2>

  <%= form_with model: @genre do |f| %>
    <h4>ジャンル名</h4>
    <%= f.text_field :name %>
     <%= f.submit '新規登録' %>
  <% end %>
  
  <h4>ジャンル</h4>
  <% @genres.each do |genre| %>

  
  <span><%= genre.name %></span><%= link_to "編集する", edit_path(genre.id) %>
  
<% end %>